# Synthetic Data Directory

This directory may contain public-safe synthetic data and machine-readable assessment contracts.

Current artifact:

- `assessment_form_spec.json`: architecture contract for the one-hour SAT Math-correlated diagnostic.

Future synthetic student and attempt files must use generated identifiers only and must not include real students, rosters, grades, submissions, Canvas exports, emails, or private school records.
